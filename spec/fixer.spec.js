// MutationObserver is not supported by jsdom, so... this needs a refactor.
// require('../fixer.js');

describe("A basic test group", function() {
  beforeEach(function() {
    // No-op
  });

  it("can run tests", function() {
    expect(true).toBe(true);
  });
});

